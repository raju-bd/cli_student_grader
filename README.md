#CLI Student Grader A sample command-line application 
#cli_student_grader

Build a command-line app in Dart that manages students, records scores, calculates grades, and prints formatted report cards.